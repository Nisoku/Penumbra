export struct BlockVM {
    id: int,
    text: string,
    edit-text: string,
    is-active: bool,
    kind: string,
    level: int,
    language: string,
    live-kind: string,
    live-text: string,
    live-height: length,
}

export struct NoteCardVM {
    id: int,
    title: string,
    preview: string,
    tags: string,
    pinned: bool,
    x: length,
    y: length,
}
